name: proton

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    # strategy:
    #   matrix:
    #     architectures: ${{ vars.ARCHITECTURES }}
    steps:
      - run: |
          echo ${{ vars.ARCHITECTURES }}
