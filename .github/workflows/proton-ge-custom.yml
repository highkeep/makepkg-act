name: proton

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        arch: ${{ fromJSON(vars.ARCHITECTURES) }}
    steps:
      - run: |
          echo ${{ matrix.arch }}
